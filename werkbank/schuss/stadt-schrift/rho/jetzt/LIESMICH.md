# Arm "jetzt" — Rohdaten fuer schnitte.py

Die Dateien heissen hier `e<epoche>-<buchstabe>.json`, weil
`werkbank/schuss/fuhre-w6/schnitte.py` genau dieses Muster sucht. Kopiert aus
`rho/jetzt-eN.json`.

**Ordner ohne versionierte Datei ueberleben den Container-Reset nicht** — Git
kennt keine leeren Ordner. Deshalb diese Datei.
