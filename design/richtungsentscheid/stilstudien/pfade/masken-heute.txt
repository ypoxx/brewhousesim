Same game, same mask logic as the reference — but today: the table is a retail listing dashboard — supermarket chains as rows, shelf-meter and distribution columns, one row flagged red for delisting risk, clean flat UI with a single amber accent, the brewery's old enamel-sign logo top left as the only historic object, WEITER as a modern button bottom right. Same layout skeleton: table left, feed right.
